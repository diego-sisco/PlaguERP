import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  label: {
    color: 'black',
    fontSize: 16,
    fontWeight: '500',
    marginBottom: 8,
  },

  input: {
    color: 'black',
    borderWidth: 1,
    borderColor: '#adb5bd',
    padding: 10,
    borderRadius: 5,
    fontSize: 16,
  },

  inputGroup: {
    marginBottom: 10,
  },
});

export default styles;
